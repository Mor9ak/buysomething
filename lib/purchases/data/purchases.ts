export interface Purchase {
    id: string;
    title: string;
    date: string;
}
export const purchases: Purchase[] = [
]; // I know, that this code won't work, but because it's just a mock-up site, I've added it.